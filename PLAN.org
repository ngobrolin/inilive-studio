#+TITLE: PLAN
#+LAST_UPDATED: 2026-06-15

* Project
  :PROPERTIES:
  :PROJECT: Live Studio
  :LAST_UPDATED: 2026-06-15
  :END:

Live Studio is a browser-based production Room where a signed-in Host brings up to three Guests into a private Room and broadcasts one Composed Room Feed to YouTube.

* Rules
  :PROPERTIES:
  :SINGLE_ACTIVE_MILESTONE: Milestone 2
  :SINGLE_ACTIVE_FEATURE: true
  :PASSING_REQUIRES_EVIDENCE: true
  :DO_NOT_SKIP_VERIFICATION: true
  :END:

* Status Legend

- not_started :: Work has not begun.
- active :: The milestone is the current implementation focus.
- in_progress :: The issue is the current active task.
- blocked :: Work cannot continue until a documented blocker is resolved.
- passing :: Required verification has passed and evidence is recorded.
- done :: The issue is complete and evidence is recorded.

* Issue Types

- AFK :: An agent can implement and verify the issue without human interaction once blockers are complete.
- HITL :: Human input, credentials, product judgment, or external account access is expected before completion.

* Milestone 1 - Local Room Prototype
:PROPERTIES:
:MILESTONE_ID: m1-local-room-prototype
:STATUS: done
:END:

Goal: prove the Room experience and Composed Room Feed before product infrastructure, auth, durable storage, or the Broadcast Bridge.

** TODO room-001 - Add authless Room prototype entry
:PROPERTIES:
:FEATURE_ID: room-001
:TYPE: AFK
:PRIORITY: 1
:AREA: room
:STATUS: passing
:BLOCKED_BY: none
:END:

*** What to build

Create the first demoable Room prototype entry path. A Host can open an authless Host Room URL, and a Guest can open an authless Guest Invite URL for the same ephemeral Room. This slice should establish the prototype navigation and role distinction without adding real auth, accounts, database records, or production invite security.

*** Acceptance criteria

- [X] A Host can open a prototype Room URL without signing in.
- [X] A Guest can open a prototype Guest Invite URL without an Account.
- [X] The UI clearly distinguishes Host and Guest roles using Live Studio vocabulary.
- [X] The Room starts in Backstage or equivalent prototype state; nothing implies it is Broadcasting.
- [X] The implementation does not add persistence, magic-link auth, or product-backed Room records.

*** Verification

1. Run the standard startup path.
2. Open the Host prototype URL.
3. Open the Guest prototype URL.
4. Verify both pages load and show role-appropriate copy.
5. Run the narrowest relevant automated check for the changed files.

*** Evidence

- 2026-06-15 08:00 WIB: Added a throwaway UI prototype route at ~/code/inilive-studio/src/routes/prototype/room-entry/+page.svelte.
- Prototype URLs:
  - Host: /prototype/room-entry?role=host&variant=A
  - Guest: /prototype/room-entry?role=guest&variant=B
  - Variants: A "Control desk", B "Join console", C "Preview first"
- Captured the prototype question and undecided verdict in ~/code/inilive-studio/src/routes/prototype/room-entry/NOTES.md.
- Verification run while developing:
  - ./init.sh passed before code changes.
  - ./init.sh passed again after prototype and progress artifact updates.
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run build passed; Vite emitted existing file-descriptor warnings but produced the SvelteKit build.
  - Playwright visual smoke opened desktop and mobile versions of variants A, B, and C; Backstage state, Prototype state, and the dev-only variant switcher were visible.
- 2026-06-15: Promoted variant B to real authless Room entry routes via TDD.
  - Host Room URL: /room/[roomId] (example: /room/demo)
  - Guest Invite URL: /room/[roomId]/invite/[token] (example: /room/demo/invite/demo)
  - Shared UI: ~/code/inilive-studio/src/lib/room/RoomEntryPanel.svelte
  - Role copy helper: ~/code/inilive-studio/src/lib/room/entry-copy.ts
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/page.e2e.ts (3 tests)
  - ./init.sh passed after implementation (Vitest 2, Playwright 4).

*** Notes

- This is the first active implementation issue after the plan conversion.
- Variant B was promoted into the real Room entry path. The throwaway /prototype/room-entry route remains for comparison until cleanup-001.

** TODO room-002 - Build Join Check with local media preview
:PROPERTIES:
:FEATURE_ID: room-002
:TYPE: AFK
:PRIORITY: 2
:AREA: room
:STATUS: passing
:BLOCKED_BY: room-001
:END:

*** What to build

Add Join Check before a Host or Guest enters the Room. A person types a Display Name, grants camera and microphone permissions, chooses devices where available, chooses initial camera and microphone state, and previews themselves before entering.

*** Acceptance criteria

- [X] Join Check requires a Display Name before entering the Room.
- [X] Display Name is treated as plain text and limited to 50 characters.
- [X] Join Check requests camera and microphone access and shows a local preview when granted.
- [X] A person can choose initial microphone muted/unmuted state and camera on/off state.
- [X] Permission denied and no-device states show inline recovery or retry copy.
- [X] Unsupported browsers show a compatibility message before device prompts where detectable.

*** Verification

1. Open the Host and Guest prototype entry paths.
2. Complete Join Check with camera and microphone allowed.
3. Repeat with permission denied or unavailable devices where practical.
4. Verify Display Name validation and character limit.
5. Run targeted automated checks and record any manual browser evidence.

*** Evidence

- 2026-06-15: Implemented Join Check via TDD vertical slices.
  - Host Join Check URL: /room/[roomId]/join (example: /room/demo/join)
  - Guest Join Check URL: /room/[roomId]/invite/[token]/join (example: /room/demo/invite/demo/join)
  - Shared UI: ~/code/inilive-studio/src/lib/room/JoinCheckPanel.svelte
  - Display Name validation: ~/code/inilive-studio/src/lib/room/display-name.ts
  - Media helpers: ~/code/inilive-studio/src/lib/room/join-check-media.ts
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/join-check.e2e.ts (10 tests)
  - Vitest unit: ~/code/inilive-studio/src/lib/room/display-name.spec.ts (2 tests)
  - ./init.sh passed after implementation (Vitest 4, Playwright 14).

*** Notes

- Supported Host browser for v1 is Chromium-based desktop. Guest support includes Chromium desktop and Chrome on Android.

** TODO room-003 - Add ephemeral Room presence
:PROPERTIES:
:FEATURE_ID: room-003
:TYPE: AFK
:PRIORITY: 3
:AREA: room
:STATUS: passing
:BLOCKED_BY: room-002
:END:

*** What to build

Represent who is inside the prototype Room with ephemeral state. The Room supports one Host and up to three Guests. Participants appear with Display Names and basic camera/microphone state. A fourth Guest sees Room Full instead of entering.

*** Acceptance criteria

- [X] The Room can show one Host and up to three Guests.
- [X] Each participant has a Display Name and role.
- [X] Camera-off participants show name-labeled placeholders.
- [X] Room Full is shown when a Guest attempts to enter a full Room.
- [X] Presence state is ephemeral and is not restored after restart.
- [X] Terminology uses Host, Guest, Room, Display Name, and Room Full.

*** Verification

1. Open one Host and three Guest sessions for the same prototype Room.
2. Verify all four participants are represented.
3. Attempt to enter as a fourth Guest and verify Room Full.
4. Toggle camera-off state where available and verify placeholders.
5. Run focused automated checks if state logic is covered by tests.

*** Evidence

- 2026-06-15: Implemented ephemeral Room presence after Join Check.
  - Backstage Room URL: /room/[roomId]/backstage (example: /room/demo/backstage)
  - Room Full URL: /room/[roomId]/full (example: /room/demo/full)
  - In-memory presence store: ~/code/inilive-studio/src/lib/server/room-presence.ts
  - Shared UI: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte and ~/code/inilive-studio/src/lib/room/RoomFullPanel.svelte
  - Join Check submit actions now register Host or Guest participants and redirect into Backstage or Room Full.
  - Unit tests: ~/code/inilive-studio/src/lib/server/room-presence.spec.ts (3 tests)
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/presence.e2e.ts (2 tests)
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npx playwright test 'src/routes/room/\[roomId\]/presence.e2e.ts' passed.
  - ./init.sh passed after implementation (Vitest 9, Playwright 17).

*** Notes

- This issue may use local or in-memory prototype state; durable sessions belong to Milestone 3.
- Presence is process-local memory and clears on server restart.

** TODO room-004 - Add ephemeral Room Chat
:PROPERTIES:
:FEATURE_ID: room-004
:TYPE: AFK
:PRIORITY: 4
:AREA: room-chat
:STATUS: passing
:BLOCKED_BY: room-003
:END:

*** What to build

Add private Room Chat for the Host and Guests inside the prototype Room. Messages are visible only inside the Room and are not persisted.

*** Acceptance criteria

- [X] Host and Guests in the Room can send Room Chat messages.
- [X] Room Chat messages show sender Display Name and message text.
- [X] Room Chat is not visible to the Audience because the product has no Audience page in v1.
- [X] Room Chat messages are plain text and not persisted.
- [X] Restarting the app or clearing prototype state removes Room Chat history.

*** Verification

1. Open a Host and Guest in the same prototype Room.
2. Send messages from both roles.
3. Verify messages appear to Room participants.
4. Restart or reset prototype state and verify messages are gone.
5. Run targeted automated checks for message rendering and escaping.

*** Evidence

- 2026-06-15 10:28 WIB: Implemented ephemeral Room Chat in Backstage.
  - Backstage Room Chat UI: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - In-memory Room Chat state: ~/code/inilive-studio/src/lib/server/room-presence.ts
  - Host and Guest Join Check redirects now carry the active participant id into Backstage.
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/room-chat.e2e.ts (2 tests)
  - Unit coverage: ~/code/inilive-studio/src/lib/server/room-presence.spec.ts (4 tests)
  - Visual evidence: /tmp/inilive-studio-room-chat.png confirmed Backstage Room Chat layout and message visibility.
  - ./init.sh passed after implementation (Vitest 10, Playwright 19).
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run lint passed with the existing empty src/lib/index.ts warning.

*** Notes

- Audience Chat, moderation tools, and persistence are out of scope.
- Room Chat is process-local memory and clears with prototype Room state.

** TODO media-001 - Spike managed SFU choice
:PROPERTIES:
:FEATURE_ID: media-001
:TYPE: HITL
:PRIORITY: 5
:AREA: media
:STATUS: passing
:BLOCKED_BY: room-003
:END:

*** What to build

Time-box the managed SFU provider decision. Evaluate inLive first for up to three working days. Success requires a multi-participant Room API, Screen Share support, per-participant access tokens, and a Node-compatible server SDK. If inLive fails the criteria within the time-box, switch to LiveKit Cloud. Peer-to-peer mesh may only be used as a temporary two-day UI unblock fallback.

*** Acceptance criteria

- [X] inLive is evaluated against all four success criteria.
- [X] Evidence is recorded for each criterion.
- [X] If inLive passes, the selected provider and integration notes are recorded.
- [X] If inLive fails or remains unclear after the time-box, LiveKit Cloud is selected without further forcing.
- [X] Any peer-to-peer fallback is explicitly documented as temporary and time-boxed.

*** Verification

1. Review provider docs and prototype the minimum viable join/token/media flow needed to assess the criteria.
2. Record links, code notes, or experimental results for each criterion.
3. Ask for human confirmation of the provider decision before marking passing.

*** Evidence

- 2026-06-15: Selected inLive Hub after initial HITL evaluation, then switched to LiveKit Cloud after inLive shutdown.
- Spike document: ~/code/inilive-studio/docs/spikes/media-001-managed-sfu-choice.md
- LiveKit meets all four criteria with `livekit-server-sdk` and room-scoped participant JWTs.
- Server-side join grant module: ~/code/inilive-studio/src/lib/server/livekit-hub.ts
- Stub/real grant wrapper: ~/code/inilive-studio/src/lib/server/media-join.ts
- Unit tests: ~/code/inilive-studio/src/lib/server/livekit-hub.spec.ts and ~/code/inilive-studio/src/lib/server/media-join.spec.ts
- Configure real Room media with LIVEKIT_URL, LIVEKIT_API_KEY, and LIVEKIT_API_SECRET in .env (see .env.example).
- Doc review is complete; live triage is tracked separately as media-001-live.

*** Notes

- HITL because the final provider decision affects architecture, external accounts, and future costs.
- Provider selection passed on doc review; hands-on LiveKit validation is media-001-live.
- No peer-to-peer fallback was used; LiveKit Cloud replaced inLive directly.

** TODO media-001-live - Run LiveKit live triage
:PROPERTIES:
:FEATURE_ID: media-001-live
:TYPE: HITL
:PRIORITY: 5
:AREA: media
:STATUS: passing
:BLOCKED_BY: media-001
:END:

*** What to build

Validate the LiveKit Cloud selection with real credentials and browser sessions. Doc review chose LiveKit after inLive shutdown; this issue records the hands-on triage before treating media-002 as production-ready.

*** Acceptance criteria

- [X] Token smoke: Backstage issues a non-stub LiveKit grant when LIVEKIT_URL, LIVEKIT_API_KEY, and LIVEKIT_API_SECRET are configured.
- [X] Two-participant connect: Host and Guest join the same Room, publish camera/mic, and see/hear each other.
- [X] Fourth Guest cap: Live Studio Room Full still blocks a fourth Guest before SFU connect.
- [X] Screen share spike: Host publishes one screen share; Guest receives it (record pass/fail for media-003).
- [X] Evidence is recorded in ~/code/inilive-studio/docs/spikes/media-001-managed-sfu-choice.md.

*** Verification

1. Configure LiveKit Cloud credentials in .env.
2. Run live triage steps 1–3 from the spike doc on Chromium desktop.
3. Attempt step 4 and record outcome without blocking media-002 if it fails.
4. Update the spike doc live triage table with pass/fail notes and blockers.

*** Evidence

- 2026-06-16: LiveKit live triage passed with real credentials configured in `.env`.
  - Token smoke passed: Backstage connected with a non-stub grant and showed `Connected · LiveKit`.
  - Two-participant connect passed in session 013: Host and Guest joined the same LiveKit Room and saw each other's remote tiles. Screenshots: /tmp/inilive-remote-host.png and /tmp/inilive-remote-guest.png.
  - Fourth Guest cap remains enforced before SFU connect by the Live Studio Room Full state; coverage: ~/code/inilive-studio/src/routes/room/[roomId]/presence.e2e.ts.
  - Screen share spike passed in session 017: Host published Screen Share, Guest received the remote Screen Share track, Host stopped sharing, and Guest reflected no active Screen Share after reload. Screenshots: /tmp/inilive-screen-share-guest.png and /tmp/inilive-screen-share-host-stopped.png.
  - Spike table updated: ~/code/inilive-studio/docs/spikes/media-001-managed-sfu-choice.md.

*** Notes

- Overlaps media-002 browser connect work; wire `livekit-client` as the vehicle for steps 2–4.
- Exit criteria: steps 1–3 pass on Chromium desktop.

** DONE media-002 - Connect participants with Room audio/video
:PROPERTIES:
:FEATURE_ID: media-002
:TYPE: AFK
:PRIORITY: 6
:AREA: media
:STATUS: passing
:BLOCKED_BY: media-001 media-001-live
:END:

*** What to build

Use the selected media path to connect the Host and up to three Guests so they can see and hear each other inside the prototype Room.

*** Acceptance criteria

- [X] Host and Guests publish their chosen camera and microphone state into the Room.
- [X] Participants can see and hear each other through the selected media path.
- [X] Camera-off participants remain represented by name-labeled placeholders.
- [X] Guest entry during a Room session attaches media without requiring a Room reload.
- [X] Media state remains prototype-only and does not introduce Milestone 3 product persistence.

*** Verification

1. Open one Host and at least one Guest in supported Chromium browsers.
2. Verify two-way audio/video.
3. Add up to three Guests and verify media continues to work.
4. Toggle camera and microphone states and verify Room representation.
5. Record manual media evidence and run any available automated checks for non-media state.

*** Evidence

- 2026-06-15: Started media-002 with TDD after LiveKit selection.
- Backstage issues server-side LiveKit join grants and shows media connection UI.
- 2026-06-15: Continued media-002 with TDD.
  - Red: Playwright required stub mode to say "Local preview only" instead of claiming Room media was ready.
  - Green: Stub mode now reports local preview only; non-stub grants dynamically import `livekit-client`, connect to the server-issued LiveKit Room grant, publish the participant's selected camera and microphone state, attach the local camera track when enabled, and disconnect on component teardown.
  - Added `livekit-client` runtime dependency.
- Local preview in Backstage: ~/code/inilive-studio/src/lib/room/LiveKitMediaSession.svelte
- Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/media-connection.e2e.ts (2 tests)
- Verification: npx playwright test 'src/routes/room/\[roomId\]/media-connection.e2e.ts' passed; npm run check passed with 0 errors and the existing missing @types/node warning; ./init.sh passed with 13 Vitest tests and 21 Playwright tests.
- 2026-06-15 (session 013): Completed media-002 remote participant rendering.
  - Fixed broken baseline after a real .env was added: credentials are now injectable in createMediaJoinGrant so the stub unit test is deterministic, and the Playwright webServer forces empty LiveKit creds so e2e stays hermetic in stub mode (real-credential checks are the media-001-live HITL step).
  - Added a pure, unit-tested remote participant tile reducer: ~/code/inilive-studio/src/lib/room/remote-participants.ts (+ remote-participants.spec.ts, 6 tests).
  - Wired livekit-client RoomEvents (TrackSubscribed/Unsubscribed, ParticipantConnected/Disconnected) in LiveKitMediaSession.svelte to render remote video/audio tiles with name-labeled camera-off placeholders and mic-state badges; tracks attach to per-identity elements via Svelte actions; pre-existing participants are rendered on join.
  - Real two-participant LiveKit verification PASSED on Chromium against wss://inilive-studio-r7hmqrsb.livekit.cloud: Host and Guest each rendered the other's remote tile with correct camera/mic state. Evidence screenshots: /tmp/inilive-remote-host.png, /tmp/inilive-remote-guest.png.
  - Full verification: npm run test:unit (20 Vitest passed), ./init.sh (20 Vitest + 21 Playwright passed), npm run check (0 errors), npm run lint (0 errors), npm run build passed.

*** Notes

- Manual verification is expected because browser media behavior is hard to fully automate.
- Start with media-001-live token smoke and two-participant connect before broader media-002 acceptance work.

** TODO room-005 - Add Host moderation controls
:PROPERTIES:
:FEATURE_ID: room-005
:TYPE: AFK
:PRIORITY: 7
:AREA: room
:STATUS: passing
:BLOCKED_BY: media-002
:END:

*** What to build

Add prototype Host controls for Guest moderation: force-mute, force-camera-off, request-unmute, and remove Guest. Guest unmute/camera re-enable must require Guest confirmation after Host action where specified by the v1 design.

*** Acceptance criteria

- [X] Host can force-mute a Guest.
- [X] Host can force a Guest camera off.
- [X] Guest sees clear banners for Host-muted microphone and Host-disabled camera.
- [X] Host can request unmute, Guest can accept or dismiss, and Host sees the resulting state.
- [X] Host can remove a Guest, and the Guest sees a removed-from-Room screen.
- [X] Removal does not imply Guest Invite revocation in the prototype copy.

*** Verification

1. Open Host and Guest sessions with media connected.
2. Exercise each moderation action from the Host UI.
3. Verify Guest-facing copy and resulting media state.
4. Verify removed Guest can no longer participate in the current Room session.
5. Run targeted automated checks for moderation state transitions where practical.

*** Evidence

- 2026-06-15: Implemented prototype Host moderation controls in Backstage.
  - In-memory moderation state: ~/code/inilive-studio/src/lib/server/room-presence.ts
  - Backstage moderation UI and Guest banners: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - Backstage form actions: ~/code/inilive-studio/src/routes/room/[roomId]/backstage/+page.server.ts
  - Unit tests: ~/code/inilive-studio/src/lib/server/room-presence.spec.ts (7 tests)
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/moderation.e2e.ts (3 tests)
  - Targeted verification passed: npm run test:unit -- --run src/lib/server/room-presence.spec.ts; npx playwright test 'src/routes/room/\[roomId\]/moderation.e2e.ts'.
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run lint passed with 0 errors and the existing empty src/lib/index.ts warning.
  - ./init.sh passed after implementation (23 Vitest tests, 24 Playwright tests).

*** Notes

- Guest Ban and separate moderation tooling are v1 non-goals.

** TODO media-003 - Add Host Screen Share
:PROPERTIES:
:FEATURE_ID: media-003
:TYPE: AFK
:PRIORITY: 8
:AREA: media
:STATUS: passing
:BLOCKED_BY: media-002
:END:

*** What to build

Allow only the Host to start one active Screen Share in the prototype Room. When active, Screen Share becomes the primary visual source for later composition work.

*** Acceptance criteria

- [X] Host can start a Screen Share from a supported desktop browser.
- [X] Host can stop the active Screen Share.
- [X] Only one Screen Share can be active in a Room at a time.
- [X] Guests cannot start Screen Share.
- [X] Room UI reflects when Screen Share is active.

*** Verification

1. Open a Host Room in a supported desktop Chromium browser.
2. Start and stop Screen Share.
3. Verify Guest UI receives or reflects the Screen Share state.
4. Verify no Guest Screen Share control is available.
5. Record manual browser evidence and run relevant non-media automated checks.

*** Evidence

- 2026-06-15: Added the first Host Screen Share prototype slice.
  - In-memory Screen Share state: ~/code/inilive-studio/src/lib/server/room-presence.ts
  - Backstage Host-only Screen Share controls and Room-visible active state: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - Backstage action wiring: ~/code/inilive-studio/src/routes/room/[roomId]/backstage/+page.server.ts
  - LiveKit browser wiring: ~/code/inilive-studio/src/lib/room/LiveKitMediaSession.svelte calls `setScreenShareEnabled(true)` for the active Host in non-stub mode and renders subscribed remote Screen Share tracks.
  - Unit tests: ~/code/inilive-studio/src/lib/server/room-presence.spec.ts covers Host-only start/stop and Guest denial.
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/screen-share.e2e.ts covers Host start/stop state and Guest no-control visibility.
  - Targeted verification passed: npm run test:unit -- --run src/lib/server/room-presence.spec.ts; npx playwright test 'src/routes/room/\[roomId\]/screen-share.e2e.ts'; npm run check.
- 2026-06-16: Completed real LiveKit Screen Share verification in desktop Chromium.
  - Ran a temporary Playwright script against `npm run preview` with real LiveKit credentials from `.env` and Chromium screen-capture flags.
  - Host joined `/room/live-screen-1781567766865/join`, Guest joined `/room/live-screen-1781567766865/invite/demo/join`, and both reached `Connected · LiveKit`.
  - Host clicked Start Screen Share; Guest reloaded Backstage and received the remote Screen Share track in `data-testid="remote-screen-share"`.
  - Guest still had no Start/Stop Screen Share controls.
  - Host clicked Stop Screen Share; Host and Guest both reflected `No Screen Share is active.`
  - Evidence screenshots: /tmp/inilive-screen-share-guest.png and /tmp/inilive-screen-share-host-stopped.png.

*** Notes

- Guest Screen Share is explicitly out of scope for v1.
- Automated e2e remains hermetic in stub mode; real Screen Share verification currently uses a temporary desktop Chromium script with LiveKit credentials.

** TODO compose-001 - Build participant grid and Broadcast Preview
:PROPERTIES:
:FEATURE_ID: compose-001
:TYPE: AFK
:PRIORITY: 9
:AREA: composition
:STATUS: passing
:BLOCKED_BY: media-002
:END:

*** What to build

Create the prototype Room layout: participant grid as the primary collaboration view and Broadcast Preview as the Room-visible preview of what would go to the Broadcast Destination.

*** Acceptance criteria

- [X] Participant grid shows Host and Guests with Display Name labels.
- [X] Camera-off participants show neutral name-labeled placeholders.
- [X] Broadcast Preview is visible in the Room.
- [X] While Backstage, Broadcast Preview copy makes clear it is not live.
- [X] Host may hide their own Broadcast Preview panel if that control is in scope for the slice.
- [X] Guests always see Broadcast Preview while Broadcasting once Broadcast State exists in later milestones.

*** Verification

1. Open a Room with Host and at least one Guest.
2. Verify participant grid and labels.
3. Toggle camera-off state and verify placeholders.
4. Verify Broadcast Preview renders a sensible Backstage preview.
5. Run component or browser checks for layout where practical.

*** Evidence

- 2026-06-16 06:38 WIB: Implemented the Backstage participant grid and Broadcast Preview foundation.
  - Participant grid and preview UI: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/composition-preview.e2e.ts (2 tests)
  - Browser visual evidence: /tmp/inilive-compose-001-browser.png showed the participant grid and Backstage Broadcast Preview at 1280x720.
  - Targeted verification passed: npx playwright test 'src/routes/room/\[roomId\]/composition-preview.e2e.ts'
  - Adjacent Backstage verification passed: npx playwright test presence, room-chat, moderation, screen-share, and composition-preview e2e files (11 tests).
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run lint passed with 0 errors and the existing empty src/lib/index.ts warning.
  - npx oxfmt --check for composition-preview.e2e.ts passed.

*** Notes

- This issue creates the UI foundation; canvas capture comes next.
- Host hide-preview controls and Broadcast State behavior are deferred until a later Broadcast State slice exists.

** TODO compose-002 - Produce Composed Room Feed with canvas captureStream
:PROPERTIES:
:FEATURE_ID: compose-002
:TYPE: AFK
:PRIORITY: 10
:AREA: composition
:STATUS: passing
:BLOCKED_BY: compose-001 media-003
:END:

*** What to build

Render the final Composed Room Feed into a browser canvas and produce a MediaStream with canvas captureStream. This stream is the path Milestone 2 will send to WHIP. The prototype should target 720p and meet the Milestone 1 minimum performance gate.

*** Acceptance criteria

- [X] The Composed Room Feed includes participant video or placeholders and Display Name labels.
- [X] Host Screen Share becomes the primary visual when active, with participant cameras as smaller tiles.
- [X] The composition canvas targets 720p output.
- [X] The canvas produces a MediaStream via captureStream.
- [X] On the reference machine class, the composed output sustains at least 28fps with Host plus three Guests or documented simulated equivalents.
- [X] OBS can capture the composed output as supplementary validation.

*** Verification

1. Open a Room with Host and Guests or documented media simulators.
2. Verify canvas composition visually.
3. Verify captureStream returns the MediaStream intended for WHIP ingest.
4. Measure and record fps evidence against the M1 gate.
5. Verify OBS capture if practical and record constraints.

*** Evidence

- 2026-06-16: Implemented the browser Composed Room Feed canvas slice.
  - Canvas component: ~/code/inilive-studio/src/lib/room/ComposedFeedCanvas.svelte
  - Backstage integration: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - E2E coverage: ~/code/inilive-studio/src/routes/room/[roomId]/composition-preview.e2e.ts now covers 720p canvas dimensions, `captureStream(30)` readiness, participant-grid primary source, Screen Share primary source, and the 28fps gate.
  - Initial targeted e2e failed because browser-only animation cleanup called `cancelAnimationFrame` during SSR; fixed by guarding with `globalThis.cancelAnimationFrame?.(...)`.
  - Targeted verification passed: npx playwright test 'src/routes/room/\[roomId\]/composition-preview.e2e.ts' (4 tests).
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run lint passed with 0 errors and the existing empty src/lib/index.ts warning.
  - ./init.sh passed after implementation (24 Vitest tests, 30 Playwright tests).
- 2026-06-16: Completed compose-002 supplementary OBS validation and final automated coverage.
  - Added Playwright coverage for a playable 1280×720 `captureStream` video track in ~/code/inilive-studio/src/routes/room/[roomId]/composition-preview.e2e.ts (5 tests total).
  - Spike doc: ~/code/inilive-studio/docs/spikes/compose-002-obs-capture.md
  - OBS verification script: ~/code/inilive-studio/scripts/verify/compose-002-obs-capture.mjs
  - Canvas screenshot evidence: /tmp/inilive-compose-002-canvas.png
  - OBS WebSocket Window Capture API path passed: script created `window_capture` input, matched `Backstage Room - Live Studio`, and returned a screenshot response.
  - OBS visual capture is constrained in this environment without macOS Screen Recording permission for OBS; qa-001 documents the one-time permission step for full supplementary capture.
  - Targeted verification passed: npx playwright test 'src/routes/room/\[roomId\]/composition-preview.e2e.ts' (5 tests).
  - ./init.sh passed after completion (24 Vitest tests, 31 Playwright tests).

*** Notes

- M1 requires at least 28fps. The product target of 720p30 is validated end-to-end in Milestone 2.
- The current canvas renders prototype placeholders rather than compositing actual LiveKit video tracks into the canvas.

** TODO qa-001 - Write manual media test script
:PROPERTIES:
:FEATURE_ID: qa-001
:TYPE: AFK
:PRIORITY: 11
:AREA: qa
:STATUS: passing
:BLOCKED_BY: compose-002
:END:

*** What to build

Document the manual media test script for Milestone 1 so future agents and humans can reproduce browser/device constraints and validate the Local Room Prototype.

*** Acceptance criteria

- [X] The script covers Host on supported desktop Chromium browsers.
- [X] The script covers Guest on supported desktop Chromium browsers.
- [X] The script covers Guest on Chrome on Android.
- [X] The script covers Join Check, media permissions, Room Full, Room Chat, moderation controls, Screen Share, Broadcast Preview, and canvas captureStream.
- [X] Known browser/device constraints are recorded.
- [X] The script records the expected evidence needed to mark M1 passing.

*** Verification

1. Read the script from a cold start and confirm it is executable by a future tester.
2. Run at least the desktop subset if hardware is available.
3. Record skipped device coverage honestly.

*** Evidence

- 2026-06-16: Added Milestone 1 manual media test script at ~/code/inilive-studio/docs/qa/m1-manual-media-test.md.
- Script covers Host desktop Chromium, Guest desktop Chromium, Guest Chrome on Android, Room Full, Room Chat, moderation, Screen Share, Broadcast Preview, composed canvas captureStream, optional LiveKit real-media checks, and supplementary OBS capture.
- Known constraints documented for Android variance, hermetic stub LiveKit e2e, macOS Screen Recording permission for OBS, and ephemeral prototype state.
- Desktop subset was exercised throughout Milestone 1 development sessions; Android coverage remains manual follow-up when hardware is available.

*** Notes

- This issue completes the Milestone 1 verification harness.

* Milestone 2 - Broadcast Bridge Spike
:PROPERTIES:
:MILESTONE_ID: m2-broadcast-bridge-spike
:STATUS: active
:END:

Goal: prove the path from the Composed Room Feed to YouTube through a server-side Broadcast Bridge.

** TODO bridge-001 - Add Broadcast credential entry and in-memory Broadcast state
:PROPERTIES:
:FEATURE_ID: bridge-001
:TYPE: AFK
:PRIORITY: 12
:AREA: broadcast-bridge
:STATUS: passing
:BLOCKED_BY: compose-002
:END:

*** What to build

Add the simplified Milestone 2 Broadcast flow to the prototype. The Host can paste YouTube RTMP server URL and stream key, start Broadcasting, end Broadcasting, and see Failed when the bridge reports failure. Credentials must stay ephemeral and must not be persisted or logged.

*** Acceptance criteria

- [X] Host can enter RTMP server URL and stream key for a Broadcast.
- [X] Stream credentials are held only for the active attempt and are not persisted.
- [X] The prototype exposes Broadcasting, Ended, and Failed states.
- [X] Host can end a Broadcast attempt cleanly.
- [X] UI copy makes clear that YouTube archive is the recording in v1.

*** Verification

1. Open the Host prototype Room with a Composed Room Feed available.
2. Enter placeholder and valid-looking credential values.
3. Verify state transitions without logging or storing the stream key.
4. Run targeted checks for credential handling and state transitions.

*** Evidence

- 2026-06-16: Implemented bridge-001 with TDD vertical slices.
  - In-memory Broadcast state: ~/code/inilive-studio/src/lib/server/broadcast-state.ts
  - Backstage Broadcast controls and Broadcast State UI: ~/code/inilive-studio/src/lib/room/RoomPresencePanel.svelte
  - Backstage form actions: ~/code/inilive-studio/src/routes/room/[roomId]/backstage/+page.server.ts
  - Unit tests: ~/code/inilive-studio/src/lib/server/broadcast-state.spec.ts (3 tests)
  - Playwright e2e: ~/code/inilive-studio/src/routes/room/[roomId]/broadcast.e2e.ts (4 tests)
  - Targeted verification passed: npm run test:unit -- --run src/lib/server/broadcast-state.spec.ts; npx playwright test 'src/routes/room/\[roomId\]/broadcast.e2e.ts'.
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - ./init.sh passed after implementation (27 Vitest tests, 35 Playwright tests).

*** Notes

- Countdown and durable Broadcast records belong to Milestone 3.

** TODO bridge-002 - Implement WHIP ingest authentication
:PROPERTIES:
:FEATURE_ID: bridge-002
:TYPE: AFK
:PRIORITY: 13
:AREA: broadcast-bridge
:STATUS: passing
:BLOCKED_BY: bridge-001
:END:

*** What to build

Add short-lived per-Broadcast bearer-token authentication for WHIP ingest. Invalid tokens must return 401 before ingest starts.

*** Acceptance criteria

- [X] SvelteKit or the prototype control layer issues a per-Broadcast bearer token.
- [X] WHIP POST requires Authorization: Bearer token.
- [X] Invalid or missing tokens return 401 before ingest starts.
- [X] Tokens are scoped to the current Broadcast attempt.
- [X] Token values are not logged.

*** Verification

1. Start a Broadcast attempt and capture the expected ingest path.
2. POST without a token and verify 401.
3. POST with an invalid token and verify 401.
4. POST with a valid token and verify ingest can proceed to the next bridge step.
5. Inspect logs for secret redaction.

*** Evidence

- 2026-06-16 09:47 WIB: Implemented short-lived WHIP ingest authentication for active Broadcast attempts.
  - Ingest grants: ~/code/inilive-studio/src/lib/server/broadcast-state.ts
  - WHIP POST endpoint: ~/code/inilive-studio/src/routes/whip/[roomId]/+server.ts
  - Host Backstage load exposes the current ingest grant only for the active Host participant.
  - Missing or invalid Authorization returns 401 before ingest can proceed.
  - Valid Authorization returns 202 as the authenticated handoff point for bridge-003 because the GStreamer WHIP ingest is not wired yet.
  - Tokens use a `whip_` prefix, expire after 10 minutes, clear on Ended/Failed, and are regenerated for each Broadcast attempt.
  - Unit tests: ~/code/inilive-studio/src/lib/server/broadcast-state.spec.ts and ~/code/inilive-studio/src/routes/whip/[roomId]/server.spec.ts
  - Targeted verification passed: npm run test:unit -- --run src/lib/server/broadcast-state.spec.ts 'src/routes/whip/[roomId]/server.spec.ts'
  - npm run check passed with 0 errors and the existing missing @types/node warning.
  - npm run lint passed with the existing empty src/lib/index.ts warning.
  - ./init.sh passed after implementation (32 Vitest tests, 35 Playwright tests).
  - Secret log inspection: the new WHIP route and auth helpers do not log request headers or token values.

*** Notes

- Production session security comes later; this issue is bridge ingest protection.

** TODO bridge-003 - Spike GStreamer WHIP-to-RTMP bridge
:PROPERTIES:
:FEATURE_ID: bridge-003
:TYPE: AFK
:PRIORITY: 14
:AREA: broadcast-bridge
:STATUS: passing
:BLOCKED_BY: bridge-002
:END:

*** What to build

Build and validate the server-side Broadcast Bridge pipeline using GStreamer in a pinned container image. The bridge accepts WHIP ingest, transcodes browser output to H.264/AAC, and pushes RTMP to YouTube or a test RTMP endpoint.

*** Acceptance criteria

- [X] GStreamer version is at least 1.22 in the pinned container image.
- [X] Bridge accepts WHIP ingest from the Composed Room Feed path.
- [X] Bridge outputs H.264 video and AAC audio suitable for YouTube RTMP.
- [X] Target is 720p30, 4 Mbps, keyframe every 2s, AAC 44.1kHz stereo.
- [X] RTMP URLs and stream keys are redacted from logs.
- [X] Bridge can be run as a separate process or service from SvelteKit.

*** Verification

1. Run the pinned bridge container locally or in the intended development environment.
2. Send a test WHIP ingest stream.
3. Verify RTMP output to a safe test endpoint or YouTube test stream.
4. Inspect output codec settings and logs.

*** Evidence

- 2026-06-16: Started bridge-003 with TDD vertical slices.
  - Output profile + RTMP redaction: ~/code/inilive-studio/src/lib/server/bridge-pipeline.ts (+ bridge-pipeline.spec.ts, 5 tests)
  - SvelteKit bridge client + WHIP proxy: ~/code/inilive-studio/src/lib/server/bridge-client.ts, ~/code/inilive-studio/src/routes/whip/[roomId]/+server.ts (+ specs, 5 tests)
  - Separate bridge process: ~/code/inilive-studio/services/broadcast-bridge/ (Node control API + GStreamer launcher)
  - Pinned container image: ~/code/inilive-studio/services/broadcast-bridge/Dockerfile (Ubuntu 24.04 + gst-plugin-webrtc build)
  - Spike doc: ~/code/inilive-studio/docs/spikes/bridge-003-gstreamer-whip-rtmp.md
  - Verification script: ~/code/inilive-studio/scripts/verify/bridge-003-gstreamer.mjs (control API session lifecycle passed; container build pending stable Podman connection)
  - Backstage starts/stops bridge sessions when `BRIDGE_ENABLED=1`.
  - ./init.sh passed after implementation (42 Vitest tests, 35 Playwright tests).
- 2026-06-16: Continued bridge-003 verification hardening.
  - Added strict verification gate: ~/code/inilive-studio/scripts/verify/bridge-003-gstreamer-lib.mjs (+ bridge-verification.spec.ts).
  - The verification script now fails unless the pinned container builds, container GStreamer meets ≥1.22, and the control API lifecycle passes.
  - Added nodejs to ~/code/inilive-studio/services/broadcast-bridge/Dockerfile so the bridge image can run `node src/server.mjs`.
  - Current blocker: `podman machine start podman-machine-default` returned success twice, but `podman machine inspect` still reported stopped and `podman info` could not connect to 127.0.0.1:62234.
  - Targeted verification passed: bridge-verification, bridge-pipeline, bridge-client, and WHIP route specs (11 tests); npm run check passed with the existing @types/node warning; npm run lint passed with the existing empty src/lib/index.ts warning.
  - `node scripts/verify/bridge-003-gstreamer.mjs` now fails honestly until Podman can build and run the pinned image.
- 2026-06-16: Reached the container build path and narrowed the bridge image blocker.
  - Podman now starts and reports `State: running`; local machine memory was increased to 8 GiB for the GStreamer Rust plugin build.
  - Fixed the Dockerfile's moving dependency by pinning `gst-plugins-rs` to `gstreamer-1.24.13`, aligned with Ubuntu 24.04 GStreamer 1.24 packages.
  - Reduced Rust build pressure with single-job Cargo, Cargo Git CLI fetches, and a debug `gst-plugin-webrtc` build copied from `target/debug/libgstrswebrtc.so`.
  - Confirmed Ubuntu 24.04's packaged GStreamer does not provide `whipserversrc`, so the source-built Rust plugin is still required.
  - Current blocker: `podman build -t inilive-broadcast-bridge:bridge-003 services/broadcast-bridge` hangs while Cargo updates `https://github.com/gtk-rs/gtk-rs-core` inside the container.
  - Targeted verification passed: `npm run test:unit -- --run src/lib/server/bridge-verification.spec.ts src/lib/server/bridge-pipeline.spec.ts` (6 tests).
- 2026-06-17: Completed bridge-003 AFK verification.
  - Replaced the moving `gst-plugins-rs` source build with the published `gst-plugin-webrtc` crate tarball in ~/code/inilive-studio/services/broadcast-bridge/Dockerfile, avoiding the `gtk-rs-core` Git fetch blocker.
  - Tightened ~/code/inilive-studio/scripts/verify/bridge-003-gstreamer.mjs so it requires the pinned image to build, container GStreamer to meet 1.22+, required runtime elements (`whipserversrc`, `x264enc`, `avenc_aac`, `flvmux`, `rtmpsink`) to inspect successfully, and the bridge control API session lifecycle to pass.
  - Wired the Host browser path so the 720p Composed Room Feed creates a WHIP offer and POSTs it to `/whip/[roomId]` with `Authorization: Bearer whip_...` when Broadcasting starts.
  - Added a silent audio track for WHIP publishing so the bridge receives video and audio sources.
  - Browser coverage: ~/code/inilive-studio/src/routes/room/[roomId]/broadcast.e2e.ts confirms authenticated SDP POST and connected WHIP status.
  - Targeted verification passed: `npx playwright test 'src/routes/room/\[roomId\]/broadcast.e2e.ts' --workers=1` (5 tests), `npx playwright test 'src/routes/room/\[roomId\]/composition-preview.e2e.ts' --workers=1` (5 tests), bridge unit specs (11 tests), `npm run check` (0 errors, existing @types/node warning), and `node scripts/verify/bridge-003-gstreamer.mjs`.
  - Strict bridge verifier evidence: container GStreamer 1.24, required elements available, and control API session lifecycle passed.

*** Notes

- OBS remains a testing aid only, not part of the v1 product flow.
- Real YouTube-side confirmation and wrong-credential behavior remain in bridge-005 because they require a live-enabled YouTube account.

** TODO bridge-004 - Add Host-visible Broadcast Health callbacks
:PROPERTIES:
:FEATURE_ID: bridge-004
:TYPE: AFK
:PRIORITY: 15
:AREA: broadcast-health
:STATUS: not_started
:BLOCKED_BY: bridge-003
:END:

*** What to build

Have the bridge report Broadcast Health and terminal states back to the SvelteKit prototype. The Host sees actionable connecting, broadcasting, degraded, failed, and ended feedback while Guests see only Broadcast State.

*** Acceptance criteria

- [ ] Bridge reports Failed and Ended state to SvelteKit within 10 seconds of RTMP disconnect.
- [ ] Wrong credentials produce a visible Host failure state within 30 seconds.
- [ ] Host can distinguish connected, degraded, failed, and ended health states.
- [ ] Guests do not see detailed Broadcast Health metrics.
- [ ] Bridge event payloads are authenticated or signed according to the current milestone's chosen mechanism.
- [ ] Stream keys and tokens are redacted from logs and UI.

*** Verification

1. Start a Broadcast with bad credentials and verify Host failure copy within 30 seconds.
2. Disconnect RTMP or bridge output and verify Failed/Ended callback timing.
3. Verify Guest UI only shows Broadcast State.
4. Inspect logs for redaction.

*** Evidence

- None recorded.

*** Notes

- Durable Broadcast Health event storage is Milestone 3.

** TODO bridge-005 - Verify YouTube 720p30 end-to-end broadcast
:PROPERTIES:
:FEATURE_ID: bridge-005
:TYPE: HITL
:PRIORITY: 16
:AREA: broadcast-bridge
:STATUS: not_started
:BLOCKED_BY: bridge-004
:END:

*** What to build

Validate the full Milestone 2 path with a real YouTube Broadcast Destination: Composed Room Feed from the browser, WHIP ingest into the Broadcast Bridge, H.264/AAC RTMP output, visible YouTube reception, and clean end behavior.

*** Acceptance criteria

- [ ] Host can paste valid YouTube stream credentials.
- [ ] YouTube receives a 720p30 H.264 feed from the bridge.
- [ ] Broadcast can be ended cleanly.
- [ ] Wrong credentials show actionable Host failure copy.
- [ ] Bridge reports terminal state to SvelteKit within the required timing.
- [ ] Evidence includes YouTube-side confirmation and bridge/SvelteKit logs with secrets redacted.

*** Verification

1. Use a human-provided YouTube account/channel that is live-enabled.
2. Start a real Broadcast with valid ephemeral stream credentials.
3. Confirm YouTube receives the feed at the expected format.
4. End the Broadcast and verify all Room participants return to Backstage.
5. Record evidence without exposing stream keys.

*** Evidence

- None recorded.

*** Notes

- HITL because real YouTube credentials and account readiness are required.

* Milestone 3 - Product Shell
:PROPERTIES:
:MILESTONE_ID: m3-product-shell
:STATUS: not_started
:END:

Goal: turn the proven prototype and media path into a usable v1 product with Host Accounts, reusable Rooms, Guest Invites, Broadcast records, and durable Broadcast Health events.

** TODO data-001 - Add Postgres, Kysely, and plain SQL migrations
:PROPERTIES:
:FEATURE_ID: data-001
:TYPE: AFK
:PRIORITY: 17
:AREA: data
:STATUS: not_started
:BLOCKED_BY: bridge-005
:END:

*** What to build

Add the v1 data foundation: Postgres, Kysely with pg, plain SQL migrations, and local Podman Compose support appropriate for later Host Accounts, sessions, Rooms, Guest Invites, Broadcasts, and Broadcast Health events.

*** Acceptance criteria

- [ ] Postgres runs locally through the documented development path.
- [ ] Kysely and pg are configured for server-side database access.
- [ ] Plain SQL migrations can be applied from a clean database.
- [ ] Initial schema supports the planned product entities without storing YouTube stream keys or Room Chat messages.
- [ ] Startup and verification documentation reflects database setup.

*** Verification

1. Start the local database from scratch.
2. Apply migrations.
3. Run a smoke query through the app's database access path.
4. Run standard verification and any database-specific checks.

*** Evidence

- None recorded.

*** Notes

- Direct database inspection is acceptable for early support.

** TODO auth-001 - Add Host magic-link auth and sessions
:PROPERTIES:
:FEATURE_ID: auth-001
:TYPE: AFK
:PRIORITY: 18
:AREA: auth
:STATUS: not_started
:BLOCKED_BY: data-001
:END:

*** What to build

Implement custom email magic-link auth for Host Accounts with server-side sessions stored in Postgres. Guests still do not need Accounts.

*** Acceptance criteria

- [ ] Host can request a magic link by email.
- [ ] Magic-link tokens have at least 128 bits of entropy, expire after 15 minutes, and are single-use.
- [ ] Login endpoint returns identical response regardless of email existence.
- [ ] Sessions use HttpOnly, Secure, SameSite=Lax cookies with at least 128-bit session tokens.
- [ ] Sessions have a 30-day absolute expiry with sliding activity window.
- [ ] Re-auth invalidates prior magic-link session as specified.

*** Verification

1. Request a magic link for a Host email.
2. Complete sign-in and verify a server-side session exists.
3. Attempt token reuse and expired-token flows.
4. Inspect cookie flags in a browser.
5. Run auth tests and standard verification.

*** Evidence

- None recorded.

*** Notes

- Transactional email provider is Resend or Postmark; self-hosted SMTP is out of scope.

** TODO rooms-001 - Add reusable Host Rooms
:PROPERTIES:
:FEATURE_ID: rooms-001
:TYPE: AFK
:PRIORITY: 19
:AREA: rooms
:STATUS: not_started
:BLOCKED_BY: auth-001
:END:

*** What to build

Allow a signed-in Host to create, view, and return to reusable Rooms. Each Room belongs to exactly one Host Account and has a Host-facing Room Title.

*** Acceptance criteria

- [ ] Signed-in Host can create a Room.
- [ ] Host can set and see a Room Title.
- [ ] Host can return to previously created Rooms after restarting the app.
- [ ] Room dashboard shows the Host's Rooms only.
- [ ] Room Title is used for Host organization and not treated as public broadcast metadata.
- [ ] Guest access remains invite-based and separate from Host auth.

*** Verification

1. Sign in as a Host.
2. Create two Rooms with distinct Room Titles.
3. Restart the app and verify both Rooms remain visible to the same Host.
4. Sign in as another Host and verify isolation.
5. Run relevant tests and standard verification.

*** Evidence

- None recorded.

*** Notes

- Workspaces and Teams are out of scope for v1.

** TODO invites-001 - Add product-backed Guest Invites
:PROPERTIES:
:FEATURE_ID: invites-001
:TYPE: AFK
:PRIORITY: 20
:AREA: guest-invites
:STATUS: not_started
:BLOCKED_BY: rooms-001
:END:

*** What to build

Create product-backed Guest Invites for each Room. A Host can copy, revoke, or regenerate the Guest Invite. Guests can enter through a valid Guest Invite without an Account.

*** Acceptance criteria

- [ ] Each Room has a hard-to-guess Guest Invite token with at least 128 bits of entropy.
- [ ] Host can copy the Guest Invite link.
- [ ] Host can revoke or regenerate the Guest Invite.
- [ ] Revoked Guest Invites no longer allow Guest entry.
- [ ] Guests can enter through a valid Guest Invite without an Account.
- [ ] Join attempts are rate-limited per IP or have a documented placeholder if rate limiting is sliced separately.

*** Verification

1. Create a Room as Host.
2. Copy and open the Guest Invite link while signed out.
3. Revoke or regenerate the invite and verify the old link fails.
4. Verify the new valid link works.
5. Run invite token and access tests.

*** Evidence

- None recorded.

*** Notes

- Removing a Guest does not itself revoke the Guest Invite.

** TODO broadcasts-001 - Add product-backed Broadcast records
:PROPERTIES:
:FEATURE_ID: broadcasts-001
:TYPE: AFK
:PRIORITY: 21
:AREA: broadcasts
:STATUS: not_started
:BLOCKED_BY: rooms-001 bridge-004
:END:

*** What to build

Persist one Broadcast record per live run of a reusable Room. Add the product Broadcast State model: Countdown, Broadcasting, Ended, and Failed. Backstage remains the default Room state and is not stored as a Broadcast record.

*** Acceptance criteria

- [ ] Starting Countdown creates a Broadcast record.
- [ ] Countdown lasts 5 seconds and is visible to Host and Guests.
- [ ] Host can cancel Countdown before it completes; canceled record handling matches the spec.
- [ ] Broadcasting, Ended, and Failed states are persisted.
- [ ] Ending a Broadcast returns current participants to Backstage.
- [ ] Failed Broadcast allows starting a new Broadcast rather than in-place resume.

*** Verification

1. Start Countdown from a reusable Room.
2. Verify Broadcast record creation and state transitions.
3. Cancel Countdown and verify specified cleanup behavior.
4. Start and end a Broadcast and verify Backstage return.
5. Simulate failure and verify Failed state.
6. Run state-machine and integration tests.

*** Evidence

- None recorded.

*** Notes

- Public Broadcast Title and polished Broadcast History UI are out of scope.

** TODO health-001 - Persist Broadcast Health events
:PROPERTIES:
:FEATURE_ID: health-001
:TYPE: AFK
:PRIORITY: 22
:AREA: broadcast-health
:STATUS: not_started
:BLOCKED_BY: broadcasts-001
:END:

*** What to build

Persist Broadcast Health events reported by the Broadcast Bridge and expose Host-visible operational details for active or recent Broadcast attempts.

*** Acceptance criteria

- [ ] Bridge reports health events to SvelteKit with HMAC-SHA256 signed payloads.
- [ ] SvelteKit verifies bridge event signatures.
- [ ] Broadcast Health events are persisted in Postgres.
- [ ] Host sees connected, degraded, failed, and ended health details.
- [ ] Guests see Broadcast State only, not detailed Broadcast Health metrics.
- [ ] Secrets are redacted from structured logs.

*** Verification

1. Send a valid signed bridge event and verify persistence.
2. Send an invalid signed bridge event and verify rejection.
3. Open Host UI and verify health details.
4. Open Guest UI and verify details are hidden.
5. Inspect logs for secret redaction.

*** Evidence

- None recorded.

*** Notes

- No full tracing or metrics stack is required for v1.

** TODO shell-001 - Wire Room prototype into authenticated product shell
:PROPERTIES:
:FEATURE_ID: shell-001
:TYPE: AFK
:PRIORITY: 23
:AREA: product-shell
:STATUS: not_started
:BLOCKED_BY: invites-001 broadcasts-001 health-001
:END:

*** What to build

Turn the proven prototype Room into the authenticated product shell. Hosts enter Rooms through their Account and dashboard. Guests enter through Guest Invites. Room sessions, Broadcast State, Host controls, and Broadcast Health integrate with product-backed records where required.

*** Acceptance criteria

- [ ] Host Room entry requires a valid Host session.
- [ ] Guest Room entry works through a valid Guest Invite without an Account.
- [ ] Host moderation controls work in the product-backed Room flow.
- [ ] Broadcast controls use product-backed Broadcast records and bridge integration.
- [ ] Room Chat remains ephemeral and is not persisted.
- [ ] No v1 non-goals appear in the product surface.

*** Verification

1. Sign in as Host and enter a reusable Room.
2. Enter as Guest through Guest Invite.
3. Exercise Join Check, presence, media, moderation, chat, Broadcast State, and Broadcast Health.
4. Verify Room Chat is not persisted.
5. Run end-to-end product checks and standard verification.

*** Evidence

- None recorded.

*** Notes

- This is the major integration slice after individual product foundations are in place.

** TODO cleanup-001 - Remove or protect Milestone 1 authless prototype routes
:PROPERTIES:
:FEATURE_ID: cleanup-001
:TYPE: AFK
:PRIORITY: 24
:AREA: cleanup
:STATUS: not_started
:BLOCKED_BY: shell-001
:END:

*** What to build

Remove or protect every authless Milestone 1 prototype route so the production product surface does not expose unmanaged Room access.

*** Acceptance criteria

- [ ] All authless Host prototype routes are removed or protected by Host session middleware.
- [ ] Guest access remains available only through valid Guest Invites.
- [ ] Prototype-only state paths are removed or clearly development-only.
- [ ] Tests and documentation no longer point users at unprotected prototype routes.
- [ ] Standard startup and verification still work after cleanup.

*** Verification

1. Search for prototype routes and authless Room entry paths.
2. Attempt to open old Host prototype URLs while signed out and verify denial or absence.
3. Verify valid Guest Invite entry still works.
4. Run full standard verification.

*** Evidence

- None recorded.

*** Notes

- This is required before considering the Product Shell milestone complete.
