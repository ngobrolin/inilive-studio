# Use Pasted YouTube Stream Credentials for V1

V1 should let the Host paste YouTube RTMP stream credentials instead of connecting a YouTube account through OAuth. This keeps the first Room-to-YouTube pipeline focused on broadcasting the Composed Room Feed and avoids Google app verification, broadcast management APIs, quota concerns, and account-linking complexity until the core flow is proven.
