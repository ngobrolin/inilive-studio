# Use Kysely and pg for Database Access

V1 should use Kysely with the pg PostgreSQL driver for application database access. This keeps the code close to SQL and avoids an ORM model while still giving the TypeScript app compile-time help for query shapes, schema changes, and result types.
