# Use WebRTC SFU for Production Room Media

The production media path should use WebRTC with an SFU so each participant sends media once while the Room remains bandwidth-friendly as it approaches the v1 cap of one Host and three Guests. Peer-to-peer WebRTC mesh is acceptable only as a disposable prototype shortcut for validating the Room flow and Composed Room Feed; it should not define the product architecture.
